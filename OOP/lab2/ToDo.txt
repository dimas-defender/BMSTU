разный exception для итератора - get и */->
использование explicit
передавать/возвращать const T&
не нужны pop_back,front

remove/insert лист, а не только один узел
remove/insert по кол-ву узлов
cbegin, cend для константного итератора при не конст данных

бинарные операции вместо тернарных в итераторе
в ноде хранить shared_ptr<T> data?